import 'package:flutter/material.dart';

class BodyGameView extends StatelessWidget {
  const BodyGameView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}